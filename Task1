Task 1 
Вывести заданное количество случайных чисел с переходом и без перехода
на новую строку.
Scanner scanner = new Scanner(System.in);
        System.out.print("Введите Сколько вывести случайных чисел ");
        String chislo = scanner.next();
        int a = 0;
        int b = Integer.parseInt(chislo);
        for (int i=0; i<b; i++){
            int rezultat = a+(int) (Math.random()*b);
            System.out.print (" "rezultat);
            System.out.println(rezultat);

Task 2 
Ввести пароль из командной строки и сравнить его со строкой-образцом.
 Scanner scanner = new Scanner(System.in);
        System.out.print("Your password : ");
        String password1 = scanner.next();
        String password2 = "Password";
        if (password1.equals(password2))
            System.out.println("Password ok");
        else
            System.out.println("Password wrong");
